<%= form_for(@lint_item) do |f| %>
  <% if @lint_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lint_item.errors.count, "error") %> prohibited this lint_item from being saved:</h2>

      <ul>
      <% @lint_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br>
    <%= f.text_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
