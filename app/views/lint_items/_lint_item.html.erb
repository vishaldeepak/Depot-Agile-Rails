<tr>
      <td><%= lint_item.quantity %>&times;</td>
      <td><%= lint_item.product.title %></td>
      <td class="item_price"><%= number_to_currency(lint_item.total_price) %>
      </td>
      <!--We have also changed to lint_item which works thankx to rails magic -->
</tr>

